import styled from 'styled-components'
import {prop} from 'common/style'

const Text = styled.p`
  color: #b7b7b7;
  font-size: ${prop(`size`, `34px`)};
  font-weight: normal;
  margin: 0;
  min-height: 215px;
`

export default Text
