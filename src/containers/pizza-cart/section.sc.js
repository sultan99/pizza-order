import styled from 'styled-components'

const Section = styled.div`
  align-items: center;
  display: flex;

  > :first-child {
    margin-right: 24px;
    min-width: 65px;
    text-align: center;
  }
`

export default Section
